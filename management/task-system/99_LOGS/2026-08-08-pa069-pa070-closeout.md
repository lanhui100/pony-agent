# 2026-08-08 PA-069 / PA-070 收口日志

## 背景

Phase 8 未收尾项：PA-070（provider retry 与退避边界）与 PA-069-A~E（Tokio 异步后基础设施加固五卡）。任务板已标记 Done，但任务卡状态未同步（PA-069 主卡 Spec Review、子卡 Ready）。本轮完成验证、修复与任务系统收口。

## 代码验证结果（全部通过）

### PA-070 统一 Provider Request Retry 与退避边界
- `crates/pony-agent-core/src/agent/retry.rs`：`ProviderRetryPolicy` / `BackoffConfig` / `RetryBudget` / `RetryDecision` / `compute_delay` / `effective_delay`，含 FakeSleeper 确定性测试。
- `provider/mod.rs:2847` 集成 `ProviderRetryPolicy::new(config)`；`tools.rs:5165` 使用退避原语。
- 前端 whole-turn auto retry 已退场：`src/` 下无 autoRetry / wholeTurnRetry / maxRetries 残留。
- Canonical spec 存在：`openspec/specs/provider-retry-and-backoff-boundary/spec.md`。

### PA-069-A 工具文件 IO 移出 runtime 锁
- `runtime/turn_runner.rs:19-27`：`TurnContext` 携带 `tool_executor: Arc<dyn ToolExecutor + Send>`。
- 工具执行独立模块 `runtime/tool_exec.rs`（`execute_registered_tool_call` / `execute_capability_tool_call`），经 `self.tool_executor.execute(...)` 执行。
- `grep runtime\.lock()` 在 `crates/pony-agent-core/src` 生产路径无匹配。

### PA-069-B capability_registry RwLock
- `control_plane/mod.rs:846` 与 `turn_runner.rs:24` 均为 `RwLock<CapabilityRegistry>`。
- `apply_skill_source_snapshot` TOCTOU 按「读 → 释放 → 应用 → 写回重验证」修复（见 lock-ordering.md 规则 3）。

### PA-069-C Mutex 中毒容错
- 85+ 处 `unwrap_or_else(|e| { eprintln!(...); e.into_inner() })`：control_plane（graph_runs / terminal / capability_registry / sessions_rwlock）、execution_control（state）、runtime 各子模块。
- `runtime` 锁按策略保留 `.expect("runtime lock poisoned")`（10 处）——有意之举。
- 范围外残留（dispatcher / plan_state / process / turn_flow）未纳入本卡。

### PA-069-D SQLite 写路径优化
- `write_full_store` 仅用于 JSON→SQLite 迁移（`sqlite_session.rs:149`）。
- 热路径 `upsert_session()` 逐行 upsert（`sqlite_session.rs:612`），`PRAGMA wal_checkpoint(PASSIVE)`（`sqlite_session.rs:603`）。
- 专用测试：`upsert_session_updates_one_row_without_rewriting_other_sessions`。

### PA-069-E 锁序文档化
- `docs/concurrency/lock-ordering.md`（73 行）：5 级规范锁序、获取规则、已知异常（`load_session_runtime_view`）、锁清单与中毒策略。

## 修复项

历史 flaky 测试（与本轮任务无关，但阻塞全量验证）：
- `session.rs` 附件生命周期两个测试在同一毫秒内连续 4 次 `save_input_attachments`，而 `attachment_asset_id` 基于 `created_at_ms` 生成 → asset_id 碰撞覆盖。
- 修复：每次保存之间 `std::thread::sleep(2ms)` 推进时钟（仓库已有先例 `execution_control.rs:346`）。

## 验证结果

| 验证项 | 结果 |
|---|---|
| `cargo check`（shared） | 通过 |
| core lib（`-p pony-agent-core --lib`） | 733 passed / 0 failed |
| 回归集（session / provider_registry / tool_router） | 13 passed / 0 failed |
| src-tauri lib | 6 passed / 0 failed |
| 前端 vitest | 332 passed / 10 skipped |

## 任务系统收口

- PA-069 主卡：Spec Review → Done，补充收口记录与验收标准对照。
- PA-069-A~E：Ready → Done，补充实现证据与验证结果。
- 01_TASK_BOARD.md：PA-069 主卡加入 Done 区，子卡说明更新为收口完成。
- 00_DASHBOARD.md：新增 PA-069 收口条目（27），PA-070 条目补充收尾验证（26）。
- PA-070 任务卡与任务板原已为 Done，本轮补充验证确认。

## 下一步

- 无阻塞项。后续若继续扩展显式 turn-level retry，应以新 change 承接（PA-070 卡已注明）。