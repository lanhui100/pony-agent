# 2026-06-04 PA-038 / PA-039 / PA-040 Spec Review

## 审核方式

- 独立子智能体只读审阅 `PA-038 / PA-039 / PA-040` 的任务卡与 OpenSpec 文档
- 主线程根据审阅意见判断边界风险、验收缺口与可采纳修订

## 采纳的核心意见

1. `PA-038` 必须明确：run hooks 不得成为第二 arbitration source
2. `PA-039` 必须明确：`persisted_effect` 的最小 evidence schema 与 `replay_required` 的优先级
3. `PA-040` 必须明确：planner hooks 与 capability hooks 分别限权、分别验收
4. 三张卡都应补 cross-card exclusion matrix，防止范围重新回灌到 `PA-022`

## 已执行的修订

- 更新 `PA-038 / PA-039 / PA-040` 任务卡的验收标准与进展说明
- 更新三张 change 的 `design.md`
- 更新三张 change 的 `spec.md` 场景：
  - `PA-038` 新增“不得新增 execution command / 不得替代 arbitration source”
  - `PA-039` 新增“persisted evidence 最小字段 / 证据不足必须 replay_required”
  - `PA-040` 新增 planner 与 capability 各自的 transform 白名单场景

## 结论

`PA-038 / PA-039 / PA-040` 已完成一轮独立 spec 审核与采纳修订，可以作为下一轮 hooks post-foundation 近线卡的执行基线。
