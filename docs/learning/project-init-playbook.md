# 项目 Init 指导：四层记忆系统基础设施

> **状态**: v0.1 渐进构建中 —— 随「优秀软件工程项目的七个维度」课程逐课完善
> **来源**: deepseek-harness 工程体系研读 + pony-agent 四问审计实践
> **用途**: 新项目启动期的建设指导 & 既有项目的健康审查记分卡

---

## 使用方式

- **新项目启动**：按 §2 的 Day-1 清单逐项建立最小机制，宁可粗糙、不可缺席。
- **既有项目审查**：按 §3 记分卡打分（证据优先），每个缺口必须落成一个最小改进动作。
- **本文档自身**：遵守它宣讲的原则——每个章节最终都要回答"这条如何被验证"，不能只停留于散文。

---

## 1. 第一性原理（已定稿）

### P1 记忆瓶颈原理
代码只承载"是什么"；团队真正丢失的是"为什么这样"和"为什么不那样"。任何只存在于人脑或聊天记录里的决策都会随人员流动蒸发。

### P2 可验证性原理
约定的生命力 = 它的可验证性。靠自觉遵守的约定在规模和人员变化下必然衰减（实证：规则写着禁止一次性 target 目录，根目录仍躺着四个违例目录）。凡机械可查的承诺，配一条非零退出的命令。

### P3 载体分工原理
一次性判断（note）、当前事实（doc）、重复程序（skill）、恒定约束（gate）必须分开存放。四层混装，文档必腐烂。

### P6 形意分离原理
语义规范定义"什么是好的"；结构设计把语义承诺转成可检验的影子（状态入路径、哈希冻结、强制链接化）；机械门禁只锁影子、从不判断语义。机器到不了的地方（真实性、意义、价值判断），用显式标记承认盲区、用校准样例传递判断力——唯独不写假门禁制造虚假安全感。

**分派流程**：违规可凭结构/字节判定 → 门禁；违规留有下游痕迹（断链/失配）→ 先造结构再上门禁；纯判断 → 语义规范 + 校准样例；都不适用 → 诚实标注"靠自觉"。

---

## 2. 启动清单（Day-1 部分，第 1 课可写范围）

来自 dsh 的时序证据：地基和第一块砖同一天浇筑——架构不变量、质量门禁、测试策略三类根基不晚于第一个功能。

| 层 | Day-1 最小可行机制 | 升级路径 | 对应课程 |
|---|---|---|---|
| 决策 | 建 `docs/decisions/`，ADR 格式含"候选方案+取舍" | 生命周期状态机 + 强制覆盖规则 | 第 2 课 |
| 程序 | 高频操作写成脚本并入库 | 项目自著 skills（含校准样例） | 第 3 课 |
| 强制 | pre-commit 一条卫生门禁（哪怕只拦产物和大文件） | 本地快 / CI 全的分层门禁 | 第 4 课 |
| 退场 | `.gitignore` 覆盖所有产物目录；临时文件只准进 `tmp/` | 归档冻结制 + rejected 保留判据 | 第 6 课 |

**启动期三禁**：
1. 禁止"先跑起来以后再补规范"——补规范的时机永远不会到来；
2. 禁止把决策只写进聊天记录——聊完落 ADR 才算结束；
3. 禁止无证据的约定——写进文档的每条"必须"要么配命令，要么标注"暂靠自觉 + 违例即机械化"。

---

## 3. 审查记分卡（四问法）

对任何项目依次回答，**先找磁盘证据再打分，不看 README 自述**：

1. **决策记在哪？** 有没有专门载体？覆盖面靠规则还是靠自觉？被击败的选项有没有留痕？
2. **承诺怎么强制？** 文档里的"必须/禁止"有几条变成了非零退出命令？强制是单点（本地 hook）还是分层（本地+CI）？
3. **重复劳动怎么复用？** 高频+高判断力流程是口传、一次性交接文件，还是入库的可复用资产？
4. **过时的怎么退场？** 归档机制存在吗？工作区里有没有过期产物堆积（杂物堆是缺失的活证据）？

评分纪律：
- 每问 0–10 分，锚点见各课内容（随课程补充）；
- 每个缺口 → 一个**最小改进动作**（审计不产出行动清单就是吐槽）;
- 缺口之间找杠杆：**强制层是其他三层的放大器**，资源不足时优先投它。

---

## 5. 决策生命周期工程（第 2 课）

### 5.1 生命周期状态机：状态必须可机械验证

```
proposed/ ──→ implemented/ ──→ archived/
    │
    └────→ rejected/
```

- 状态编码在**路径**中（`{lifecycle}/{class}/yyyy-mm-dd-topic.md`），文件头 `Status:` 行与所在文件夹交叉验证——三重锁定（结构+内容+门禁）；
- **转换即改写**：proposed → implemented 时，`## Proposal` 必须重写为现在时 `## Decision`，验收标准/风险折叠进 `## Consequences`——移动文件夹的那次提交必须完成这次改写，门禁强制；
- **两条铁律的分离**：
  - implemented 记录的**事实**（路径、名称、默认值）必须随代码同 PR 更新——它是活的当前态描述；
  - **决定本身不可改**——推翻 = 新增记录 + 双向链接，历史不可篡改。

> 自查问题：你的决策记录里，一条记录的"死活"由什么决定？如果答案是"读者自己判断"或"索引标题里的一句话"，状态就还没有被工程化。

**路由速查（写作时四问）**：
1. 只是执行既有决定的小改动？→ 不记录；
2. 决定已做出（代码已写/在写）？→ `implemented/`（主产线，约九成变更走此）；
3. 尚需评审/验证的 substantial 工作？→ `proposed/`（少数派，无过期时间）；
4. 已否决且半年后仍会诱惑人？→ `rejected/`；否则不留痕。

时态试金石：implemented 通篇现在时（禁 Proposal/Plan 标题）；proposed 将来时；rejected 冻结提案原文。多阶段决策：已落地阶段写事实，未落地阶段不进 implemented 正文。

### 5.2 Alternatives Considered 强制制

> "A decision recorded without what it beat invites re-litigation." —— dsh notes/README.md

- 每条决策记录强制包含"候选方案与落选原因"节，格式门禁检查其存在性；
- 备选方案是决策的**有效期标签**：结论会过时，但比较记录告诉你何时该重新审视（当某个落选方案的前提条件改变时）;
- **诚实条款**：备选方案只能记录、不能编造。历史记录无据可查时，用固定标记承认空白，不许事后补造理由。

注意区分：单选项的利弊权衡（好处/代价）≠ 候选方案比较。前者回答"这个方案好不好"，后者回答"为什么不是那一个"。

### 5.3 Rejected 是护栏资产

- 保留判据不是年龄、不是字数，而是**再犯可能性**："落选方案仍是一个诱人的、有意义的错误"时才保留；
- 一条合格的拒绝记录 = 诱惑是什么 + 为什么输 + 复活条件（"等 X 出现后可重新评估"）;
- 拒绝记录的状态行是唯一允许带内容的枚举值（`rejected — <一行理由>`），因为拒绝理由正是读者来看的东西。

### 5.4 Supersession 审计（语料的 GC）

每次新增记录触发一次范围化审查：搜索覆盖同一决策的既有记录，分类完全/部分取代并**在同一变更中处理**——债务不过夜。没有它，决策语料会在几个月内腐烂成噪音。

### 5.5 最小可行改造清单（从 6 分到 9 分）

1. 每篇 ADR 加 `Status:` 行；建 `docs/decisions/rejected/` 子目录；
2. ADR 模板加必填节"候选方案与落选原因"；无据可查的历史记录标注留白而非补造；
3. 项目约定加一条：非平凡变更同一提交内带 ADR 或明确豁免说明；
4. 修复取代断链：被取代的 ADR 文件头部加 `Status: superseded by 000X` + 链接；
5. （机械化阶段）一个校验脚本：Status 行 ↔ 所在目录一致性 + superseded 链接有效性。

### 5.6 强制架构：每条保证住在哪一层

决策系统的 12 项保证按 P6 流程分派（完整分析见课程记录）：

| 保证 | 层级 | pony-agent 现状 |
|---|---|---|
| 类别合法 | 机械（封闭集合门禁） | 无分类体系 |
| 状态真实 | 结构+机械（路径+Status 行互证） | ❌ 无状态行 |
| implemented 文体 | 机械（提案时态标题禁入） | ❌ |
| 备选存在 | 机械（节存在性） | 靠自觉，4/8 达标 |
| 豁免资格 | 机械（日期许可证） | ❌ 无此设计 |
| 双语一致 | 机械（配对门禁） | 不适用（单语） |
| 引用不腐 | 结构+机械（强制相对链接+链接检查） | 无链接规范 |
| 归档冻结 | 机械终极态（append-only 哈希清单） | ❌ 无归档区 |
| 拒绝保留判据 | 语义+校准样例 | ❌ rejected 蒸发 |
| 取代及时性 | 语义（同 PR 纪律），断链由链接门禁滞后兜底 | 断链中（0004） |
| **覆盖规则** | **语义+评审——全系统最弱环，dsh 也不假装能机械化** | 判据模糊（"重大架构选择"） |
| 事实随代码更新 | 语义+评审，链接化部分有兜底 | 无规则 |

**要点**：最重要的入口规则（何时必须写 ADR）天然停在语义层——它的改进方向不是写脚本，而是把"非平凡"的判据具体化成可列举的触发条件。

### 5.7 触发机制：谁在什么时候按铃

机制的入口本身是一条散文规则，但要配两样东西才成立：

**枚举化触发条件**（防"重大"含混漏记）：行为变更 / 跨模块契约 / 推翻既有决策 / 流程与测试策略 / 新增依赖类别 / 跨模块删除——命中任一，同一提交内必须新增或更新记录；纯机械改动豁免；拿不准就写三行——多记成本一段文字，漏记成本永久丢失（成本不对称，规则故意偏宽）。

**三层规则送达**（防规则不被读到）：宪法层（根 AGENTS/AGENT.md，每次会话必载）→ 地方法规层（目录级 AGENTS.md，触碰即载）→ 程序层（skills，按任务激活）。对 AI 协作的项目，送达即强制：agent 靠上下文注入获取规则，每次会话重新生效。

五个触发入口：变更驱动（主）、提案驱动、事故驱动（postmortem→bug-fix 记录）、审计驱动（新笔记触发旧笔记审查）、维护驱动（定期归档整理）。

---

## 6. 流程资产化（第 3 课）

### 6.1 note 与 skill 的分工

| | ADR / Note | Skill |
|---|---|---|
| 承载 | 一次性判断（why + gave up） | 可重复程序（how） |
| 数量级 | 随非平凡变更增长 | 只随流程种类增长 |

note 给 skill 提供理由，skill 把理由变成动作；两者互相链接。

### 6.2 SKILL.md 解剖五要素

1. **触发式 description**：写"何时用"，不写"是什么"——它是路由条件；
2. **真相源清单**：先把本仓库的价值观装进执行者上下文；
3. **程序步骤**：机械部分写成可执行命令；
4. **校准样例**：语义部分的核心——正反例成对，每例附判定依据；
5. **验证 + 报告格式**：以可核验的报告收尾。

### 6.3 校准样例设计原则

- 样例间差异维度 = 判断的真实依据（dsh 归档样例故意让字数与结论无关，堵死按大小分类的捷径）；
- 判定线用具体事实锚定（"foundational authority"），不用形容词堆砌；
- 无判例的成文法必然被架空（"重大架构选择"→ 8 Phase 仅 8 篇 ADR）。

### 6.4 pony-agent 落地

第一个自著 skill 建议 `.agents/skills/write-adr/SKILL.md`：六条触发判据 + 四问路由 + 必备节 + 校准样例（0008 正例 / 0005 失考反例）+ Status 一致性验证。素材即本次改造的全部产出。

### 6.5 description 自足性原则（实践修正）

Skill 路由发生在"只有摘要可见"的懒加载时刻——description 中引用未读文件的内容作为触发条件（如"命中 README 六条判据时使用"）在路由时刻**不可求值**，等于没写。规则：

- **触发条件内联**进 description（用变更本身的可见特征：行为变更/契约变更/工具链变更…）；
- **权威出处放正文**（"先读契约"章节，加载后再读文件合法且必要）；
- **宪法层兜底漏触发**：AGENT.md 常载一行指向 skill，使"agent 忘想 ADR"时仍被接住——skill 触发失败 ≠ 规则失效。

### 6.6 十一技能全景补充模式（补遗课）

dsh 全部 11 个 skill 已精读。第 3 课五要素之外的新模式：

1. **调用权限分层**：`disable-model-invocation: true` = 只许用户点名、禁止模型自主路由（重型流程防误入）；
2. **输入契约**：缺 scope 就停下报告，不推断不采访；mode 控制提问不控制写权限；
3. **read, don't re-summarize**：链接权威而不复述权威——复述版会漂移；
4. **召回电池**：正则探针初筛 + 语义裁决双层，且承认"每轮都有电池漏掉的案例"，辅以无模式通读；
5. **硬停条款**：危险操作预先封死"聪明的降级路径"（工具缺失就停，不许手动绕）；
6. **证据链完整性**：一次演示=一次孤立运行，禁拼接帧；发布前重验 HEAD 未移动；
7. **被永久引用的资产获得不变性义务**：assets 分支永不重写（PR 正文永久引用其 URL）——归档哈希冻结的同源原理。

技能库按工程旅程补缺顺序（pony）：评审 > 推送检查 > 文档标准 > 找简化；原则仍是"重复到第三次再资产化"。

---

## 7. 门禁分层设计（第 4 课）

### 7.1 P7 分层强制原理

门禁按"反馈速度 × 拦截类别"分层，每层只抓该层性价比最高的缺陷；检查成本记对账本（per-commit / per-push / per-PR）；本地窄、CI 全。

### 7.2 三层预算表

| 层 | 预算 | 抓什么 | 不抓什么 |
|---|---|---|---|
| pre-commit | 秒级 | 仅暂存文件：lint 自动修复、空白、清单、尾随换行、Status 行 | 类型分析、测试、构建 |
| pre-push | 十秒级 | 增量 typecheck | 其余一切 |
| CI | 分钟级 | 穷举矩阵、100% 覆盖、克隆检测、e2e、冒烟 | —— |

记账法：typecheck 记 push 账不记 commit 账（"charges every intermediate commit instead of one push"）；两层跑同样检查 = 白付延迟。

### 7.3 证据匹配表面

改动表面决定最小充分证据：行为→聚焦测试；文档→doc-sync 等价物；模型可见输出→快照；清单/导出→build+hygiene。三禁令：不默认全量、不重复已过检查、不为遮掩降低阈值。

### 7.4 元门禁纪律

- 每条验收路径要有负样本证明（prove it rejects an invalid case）；
- 窄例外优于全局关闭；
- 绕行仅经用户同意并报告实情。

### 7.5 pony-agent 蓝图（已实施，待真实环境验证）

L1：git-guard（+尾随换行检查）+ `verify-decisions.ps1`（三项校验，已过三样本负测）；L2：改为 `npm run typecheck`，移除重复卫生检查；L3：最小 GitHub Actions。实测记录：typecheck 冷跑 ~72s（超出理想十秒预算，vue-tsc --incremental 实验无收益已回滚）→ 已知债：观察绕钩子风险，后续可选按目录拆分 tsconfig 提速。

---

## 8. 架构不变量设计（第 5 课）

### 8.1 P8 不变量经济原理

架构质量 ≈ Σ(不变量强度 × 捍卫者机械程度) ÷ 不变量数量。三过关卡：消灭哪类 bug 的存在性？谁机械捍卫？违例在哪层多快暴露？**没有捍卫者的不变量不是架构，是愿望。**

### 8.2 四种铸造工艺（dsh 标本）

| 工艺 | 不变量 | 关键句 |
|---|---|---|
| 单一真源 | 日志即状态，历史是派生 | "divergence is **structurally impossible**"——一流思维消灭 bug 存在性 |
| 接缝三角色 | Definition/Provider/Consumer；Consumer 不 import Provider | 用"变化速率与原因"定义拆分；**单提供者单消费者保持一包**（反过早抽象条款） |
| 方向感知版本 | 单调整数；写方定 bump；ignorable 默认必需 | "Parses without error is not the bar"；**错误代价不对称决定默认值方向** |
| 运行时不变量 | 只断言"跨时间/可变数据的关系" | 形状断言归类型/加载/单测；**空伴侣须带包属理由并被门禁看守**（防僵尸占位） |

### 8.3 一行不变量主题表

显式>隐式（resolve(request): Spec，禁隐藏 ?? default）· fail loud（配置错误当场爆炸，空 catch 须自白）· 边界验证同进程信任 · 注册即 effect 返回 disposer · 封闭 union assertNever · branded id · waterfall 必须 next()。

### 8.4 捍卫层分布（P6 再应用）

编译期（类型/派生函数）、加载期（版本拒绝）、运行时常驻（invariant 服务监听）、语义层（review+笔记）——同一不变量选最便宜能抓住违例的层。

---

## 9. 待解锁章节（占位——防止假装深刻）

| 章节 | 内容 | 解锁条件 |
|---|---|---|
| §9 负空间维度 | 简化、拒绝与遗忘的纪律 | 第 6 课后 |
| §9 退场与遗忘纪律 | 归档冻结制、删除判据、负空间维护 | 第 6 课后 |
| §10 实施顺序总图 | 从零到飞轮：小项目的最短路径 | 第 7 课后 |

**终态提醒**：本文档成熟后应孵化出配套脚手架（ADR 模板、gate 示例脚本、SKILL.md 模板），使"照做"不需要读文档——这是 P2 对本文档自身的应用。

---

## 版本记录

- **v0.1（第 1 课后）**：第一性原理定稿；启动清单 Day-1 部分；四问审查法；后续章节显式占位。
- **v0.2（第 2 课后）**：新增 §5 决策生命周期工程——状态机、Alternatives 强制、rejected 护栏、supersession 审计、最小改造清单。
- **v0.3（第 2 课追问后）**：新增 P6 形意分离原理与 §5.6 强制架构分层表——语义/结构/机械三层分工及 pony 逐项现状。
- **v0.4（第 2 课再追问后）**：新增 §5.7 触发机制——枚举化触发条件、成本不对称原则、三层规则送达（宪法/地方法规/程序）、五个触发入口。
- **v0.5（第 3 课后）**：新增 §6 流程资产化——note/skill 分工、SKILL.md 解剖五要素、校准样例设计原则、write-adr skill 落地方案；修复占位表重复行。
- **v0.6（用户评审修正）**：新增 §6.5 description 自足性原则——触发条件必须内联（懒加载时刻只有摘要可见）、权威出处放正文、宪法层兜底漏触发。
- **v0.7（第 4 课后）**：新增 §7 门禁分层设计——P7 原理、三层预算表、证据匹配表面与三禁令、元门禁纪律、pony 三层改造蓝图。
- **v0.8（第 4 课实践后）**：§7.5 更新为已实施状态，附 typecheck 延迟实测与增量实验回滚记录。
- **v0.9（第 3 课补遗）**：新增 §6.6 十一技能全景——7 个新设计模式（调用权限分层、输入契约、召回电池、硬停条款、证据链完整性等）；pony 技能库补缺顺序。
- **v1.0（第 5 课后）**：新增 §8 架构不变量设计——P8 不变量经济原理、四种铸造工艺（单一真源/接缝三角色/方向感知版本/运行时不变量）、一行不变量主题表、捍卫层分布。
